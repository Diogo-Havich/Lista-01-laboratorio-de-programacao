#include <stdio.h>

int main()
  {
  int valor;
  puts("digite o valor");
  scanf("%d", &valor);
  printf("valor recebido = %d\nAntecessor = %d\nSucessor = %d\n", valor, valor - 1, valor + 1);
  return 0;
  }
