#include <stdio.h>

int main ()
  {
  float conta;
  puts("digite o valor da conta");
  scanf("%f", &conta);
  printf("conta = %.2f\n10 %% = %.2f\ntotal = %.2f\n", conta, conta * 0.1, conta * 1.1);
  return 0;
  }
