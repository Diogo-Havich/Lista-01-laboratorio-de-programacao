#include <stdio.h>

int main()
	{
	float reais, cotacao;
	puts("Quantos está a cotação do dolar?");
	scanf("%f", &cotacao);
	puts("Quantos reais voce deseja converter?");
	scanf("%f", &reais);
	printf("%.2f em reais corresponde a %.2f dolares\n", reais, reais / cotacao);
	return 0;
	}
